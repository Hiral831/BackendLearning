1. aysnc function always return promise, to jaha pe bh woh call hota h try to use .then and catch 
2. Middleware :
for checking ex: check if user is admin
check if user is logged in

(err, req, res, next) 

end when we send response 
/instagram -res.send("hitesh").

jwt is a bearer token, jo usko bear krta h usko sahi mante h, jiske pass yeh token h usko hum data bhejte h

in uploading a file using multer and cloudinary sdk we are performing 2 steps settings that first store the file in local and than
in the cloudinary so as we can reattempt if upload fails  

jaha esi gunzaish h ki mamla pechida ho skta h waha try catch lgao na

export default tab hi dete h jab manchaha name se import krvana h , otherwise we'll use the name as per export given 

router alg se define kiya h express mei to usko app.js jahaa express server h waha import krke as a Middleware use krna pdega,
also remember that by default chrome use get api.